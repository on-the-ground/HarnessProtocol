# 2026 Port revision archive

This directory preserves the completed migration plans, intermediate validation snapshots, and the superseded Koog experiment. They are historical evidence and are excluded from the current library, documentation index, Gradle build, and regression counts.

The maintained contract is under `protocol/`, implementations under `implementations/`, and current verification under `verification/`. Git history remains the authoritative source for the original paths and revisions of these files.
